#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <zlib.h>

// 파일 선택 단계: 사용자로부터 파일명을 입력받아 반환하는 함수
char* selectFile() {
    char* filename = (char*)malloc(100 * sizeof(char));
    printf("Enter the filename: ");
    scanf("%s", filename);
    return filename;
}
